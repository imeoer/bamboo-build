FROM dockerfile/ubuntu
MAINTAINER "imeoer" <imeoer@gmail.com>

# Install Nodejs
RUN curl https://raw.githubusercontent.com/creationix/nvm/v0.22.2/install.sh | bash
RUN /bin/bash -c nvm install stable && nvm use stable
