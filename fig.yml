# sudo apt-get update -y
# sudo apt-get install git docker.io python-pip -y
# sudo pip install -U fig
# git clone https://github.com/imeoer/bamboo-build.git
# cd bamboo-build
# sudo fig build
# sudo fig up

api:
    build: api
    volumes:
        - ./images:/data/golang/src/github.com/imeoer/bamboo-api/public/images
    links:
        - mongodb:mongodb
    ports:
        - "80:8888"
mongodb:
    image: dockerfile/mongodb
    command: mongod --dbpath /data/db
    volumes:
        - ./database:/data/db
    ports:
        - "27017:27017"
